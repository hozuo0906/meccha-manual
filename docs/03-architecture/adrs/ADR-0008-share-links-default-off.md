# ADR-0008: 共有リンクはデフォルトOFFにする

Status: Accepted

## 決定

手順書の共有リンクはデフォルトOFFにする。作成者が明示的に有効化した場合のみ、期限、パスコード、権限範囲を設定して共有できる。

## 理由

業務手順書には社内URL、氏名、メール、顧客情報が含まれる可能性が高いため、公開リンクの暗黙有効化は情報漏えいリスクが大きい。

## 影響

- 未ログイン閲覧は共有リンク検証を必須にする。
- 生トークンは保存せずhashのみ保存する。
- 失効は即時反映する。
